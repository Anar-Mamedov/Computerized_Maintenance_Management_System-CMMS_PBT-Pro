import { Button } from "antd";
import React from "react";

export default function CustomGoButton() {
  return (
    <Button
      style={{ marginLeft: "-25px", height: "27px", display: "flex", alignItems: "center", justifyContent: "center" }}>
      Git
    </Button>
  );
}
